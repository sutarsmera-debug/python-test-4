class Book:
    def __init__(self, title, author, is_borrowed=False):
        self.title = title
        self.author = author
        self.is_borrowed = is_borrowed

        self.title = input("Enter the title of the book: ")
        self.author = input("Enter the author of the book: ")

    def borrow(self, title, author):
        if not self.is_borrowed:
            self.is_borrowed = True
            print(f"You have borrowed '{self.title}' by {self.author}.")
        else:
            print(f"'{self.title}' is already borrowed.")

        self.title2 = input("Enter the title of the book: ")
        self.author2 = input("Enter the author of the book: ")

    def return_book(self, title, author):
        if self.is_borrowed:
            self.is_borrowed = False
            print(f"You have returned '{self.title2}' by {self.author2}.")
        else:
            print(f"'{self.title2}' was not borrowed.")

obj1 = Book("The Great Gatsby", "F. Scott Fitzgerald")
obj1.borrow("The Great Gatsby", "F. Scott Fitzgerald")

obj2 = Book("To Kill a Mockingbird", "Harper Lee")
obj2.borrow("To Kill a Mockingbird", "Harper Lee"   )

obj3 = Book("1984", "George Orwell")
obj3.return_book("1984", "George Orwell")



